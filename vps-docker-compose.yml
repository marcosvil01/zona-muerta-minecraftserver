services:
  minecraft:
    image: itzg/minecraft-server:latest
    container_name: minecraft-server
    restart: unless-stopped
    ports:
      - '25565:25565'
    environment:
      EULA: 'TRUE'
      TYPE: PAPER
      VERSION: '1.21.4'
      MEMORY: 4G
      MAX_PLAYERS: 10
      MOTD: '\u00a74\u00a7l\u2620 Zona Muerta \u2620\n\u00a77Apocalipsis Zombie - Sobrevive si puedes...'
      DIFFICULTY: hard
      VIEW_DISTANCE: 10
      SIMULATION_DISTANCE: 8
      SPAWN_PROTECTION: 0
      ENABLE_COMMAND_BLOCK: 'true'
      MODE: survival
      PVP: 'true'
      ONLINE_MODE: 'true'
      SPAWN_MONSTERS: 'true'
      MAX_WORLD_SIZE: 10000
      PLAYER_IDLE_TIMEOUT: 30
      MODRINTH_PROJECTS: |
        viaversion
        viabackwards
        luckperms
        vaultunlocked
        essentialsx
        griefprevention
        coreprotect
        mythicmobs
        grappling-hook
        placeholderapi
        worldedit
        worldguard
        dynmap
        discordsrv
        invsee++
        playerkeepinventory
        tab
      MODRINTH_DOWNLOAD_DEPENDENCIES: required
    volumes:
      - minecraft-data:/data
    deploy:
      resources:
        limits:
          memory: 4608M
        reservations:
          memory: 2560M
    healthcheck:
      test: mc-health
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 180s

volumes:
  minecraft-data:
    name: minecraft-data
